using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileBase : MonoBehaviour
{
    [Header("マップオブジェクト設置用ルール")] public int settingFuncRule;
    [Header("マップ設置時の密度パラメータ")] public int denceParam;
    [Header("オブジェクト設置時の縦方向の長さ用パラメータ")] public int verticalParam;
    [Header("オブジェクト設置時の横方向の長さ用パラメータ")] public int horizontalParam;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
